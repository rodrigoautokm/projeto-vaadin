// node_modules/@vaadin/flow-frontend/messageListConnector.js
(function() {
  const tryCatchWrapper = function(callback) {
    return window.Vaadin.Flow.tryCatchWrapper(callback, "Vaadin Message List");
  };
  window.Vaadin.Flow.messageListConnector = {
    setItems: (list, items, locale) => tryCatchWrapper(function(list2, items2, locale2) {
      const formatter = new Intl.DateTimeFormat(locale2, {
        year: "numeric",
        month: "short",
        day: "numeric",
        hour: "numeric",
        minute: "numeric"
      });
      list2.items = items2.map(
        (item) => item.time ? Object.assign(item, {
          time: formatter.format(new Date(item.time))
        }) : item
      );
    })(list, items, locale)
  };
})();
//# sourceMappingURL=@vaadin_flow-frontend_messageListConnector__js.js.map
