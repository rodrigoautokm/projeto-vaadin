// node_modules/@vaadin/flow-frontend/selectConnector.js
(function() {
  const tryCatchWrapper = function(callback) {
    return window.Vaadin.Flow.tryCatchWrapper(callback, "Vaadin Select");
  };
  window.Vaadin.Flow.selectConnector = {
    initLazy: (select) => tryCatchWrapper(function(select2) {
      const _findListBoxElement = tryCatchWrapper(function() {
        for (let i = 0; i < select2.childElementCount; i++) {
          const child = select2.children[i];
          if ("VAADIN-SELECT-LIST-BOX" === child.tagName.toUpperCase()) {
            return child;
          }
        }
      });
      if (select2.$connector) {
        return;
      }
      select2.$connector = {};
      select2.renderer = tryCatchWrapper(function(root) {
        const listBox = _findListBoxElement();
        if (listBox) {
          if (root.firstChild) {
            root.removeChild(root.firstChild);
          }
          root.appendChild(listBox);
        }
      });
    })(select)
  };
})();
//# sourceMappingURL=@vaadin_flow-frontend_selectConnector__js.js.map
